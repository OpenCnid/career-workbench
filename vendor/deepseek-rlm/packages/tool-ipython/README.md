# @deepseek-rlm/dsh-tool-ipython

Registers the exclusive native DSH `ipython` tool against `ctx.rlm`. The tool preserves the outer DSH tool pipeline and its session logging. Its prompt guidance states the persistence model, explicit child-report contract, and the fact that IPython has OS authority rather than sandbox isolation.
